class Shotgun extends Weapon {
    constructor() {
        super(aa) //Weapon constructor
    }

    shoot(){
        super.shoot()
        //Get the vector of the spaceship facing rn currently xd
        //Create a new bullet with the same vector but...
        //Add an angle to the bullet random(-angle, angle)
    }
}